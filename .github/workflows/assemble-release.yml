# Created by Indra Mahkota
name: Assemble Release

on:
  workflow_run:
    workflows: ["Scan with Detekt"]
    types:
      - completed

# Add specific environment from secret
env:
  GITHUB_USERNAME: ${{ secrets.USERNAME }}
  GITHUB_TOKEN: ${{ secrets.TOKEN }}
  SSHK: ${{ secrets.SUB_SSHK_A }}

jobs:
  build-project:
    name: Build Project
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Get Submodules
        run: |
          mkdir -p $HOME/.ssh
          echo "$SSHK" > $HOME/.ssh/ssh.key
          chmod 600 $HOME/.ssh/ssh.key
          export GIT_SSH_COMMAND="ssh -i $HOME/.ssh/ssh.key"
          git submodule update --init --recursive

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2

      - name: Setup Secrets
        run: |
          mkdir -p ./keystore
          echo "${{ secrets.BASE64_CERT }}" | base64 -di > ./keystore/release-key.jks
          echo "${{ secrets.SECRET_PROPERTIES }}" > ./secrets.properties

      - name: Run Build
        run: chmod +x gradlew && ./gradlew --no-configuration-cache build

  generate-release-apk:
    needs: [build-project]
    name: Generate Release Apk
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Get Submodules
        run: |
          mkdir -p $HOME/.ssh
          echo "$SSHK" > $HOME/.ssh/ssh.key
          chmod 600 $HOME/.ssh/ssh.key
          export GIT_SSH_COMMAND="ssh -i $HOME/.ssh/ssh.key"
          git submodule update --init --recursive

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2

      - name: Setup Secrets
        run: |
          mkdir -p ./keystore
          echo "${{ secrets.BASE64_CERT }}" | base64 -di > ./keystore/release-key.jks
          echo "${{ secrets.SECRET_PROPERTIES }}" > ./secrets.properties

      - name: Run Assemble Release
        run: chmod +x gradlew && ./gradlew --no-configuration-cache releaseRenamedAPK

      - name: Get Renamed Output
        id: renamed
        run: |
          apk_location=$(cat app/exploration/build/outputs/apk/release/deliverable.txt)
          filename=$(basename $apk_location)
          echo "apkName=$filename" >> $GITHUB_OUTPUT
          echo "apkLocation=$apk_location" >> $GITHUB_OUTPUT

      - name: Upload Artifact
        uses: actions/upload-artifact@v3
        with:
          name: ${{ steps.renamed.outputs.apkName }}
          path: ${{ steps.renamed.outputs.apkLocation }}
